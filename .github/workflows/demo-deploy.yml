# Demo VM deploy (docker compose).
#
# Flow: build VitePress docs (DOCS_BASE_PATH=/) on the runner -> rsync static
# assets to the VM -> SSH: pull repo, inject secrets (GH Secrets -> /etc),
# `docker compose up -d --build`, then gate on a live health check.
#
# Required GH Secrets:
#   DEMO_SSH_HOST  VM public IP or hostname
#   DEMO_SSH_USER  SSH user (with docker group + write access to /opt/hikoutei-website)
#   DEMO_SSH_KEY   private deploy key
#   DEMO_SA_JSON   full demo service-account key JSON
#   DEMO_ENV       demo env file body (HIKOUTEI_SYNC_SPREADSHEET_URL,
#                  GOOGLE_APPLICATION_CREDENTIALS=/etc/hikoutei-demo/sa.json,
#                  PORT=3101)

name: demo-deploy

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "demo/**"
      - "qa/**"
      - ".vitepress/**"
      - "guide/**"
      - "package.json"
      - "package-lock.json"

concurrency:
  group: demo-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build docs (bare-root, demo included)
        run: npm ci && DOCS_BASE_PATH=/ npm run docs:build

      - name: Pack static assets
        run: tar -czf hikoutei-docs.tar.gz -C .vitepress/dist .

      - name: Copy static assets to VM
        uses: appleboy/scp-action@v1
        with:
          host: ${{ secrets.DEMO_SSH_HOST }}
          username: ${{ secrets.DEMO_SSH_USER }}
          key: ${{ secrets.DEMO_SSH_KEY }}
          source: hikoutei-docs.tar.gz
          target: /tmp
          strip_components: 0

      - name: Deploy stack on VM
        uses: appleboy/ssh-action@v1
        env:
          REF: ${{ github.ref_name }}
          DEMO_SA_JSON: ${{ secrets.DEMO_SA_JSON }}
          DEMO_ENV: ${{ secrets.DEMO_ENV }}
          # QA reuses the demo SA key (quota shared); only the sheet differs.
          QA_SYNC_SPREADSHEET_URL: ${{ secrets.QA_SYNC_SPREADSHEET_URL }}
          QA_SA_JSON: ${{ secrets.DEMO_SA_JSON }}
        with:
          host: ${{ secrets.DEMO_SSH_HOST }}
          username: ${{ secrets.DEMO_SSH_USER }}
          key: ${{ secrets.DEMO_SSH_KEY }}
          envs: REF,DEMO_SA_JSON,DEMO_ENV,QA_SYNC_SPREADSHEET_URL,QA_SA_JSON
          script: |
            set -euo pipefail
            if [ ! -d /opt/hikoutei-website/.git ]; then
              sudo mkdir -p /opt/hikoutei-website
              sudo chown "$USER" /opt/hikoutei-website
              git clone https://github.com/ManddarinShop/Hikoutei-Website-.git /opt/hikoutei-website
            fi
            cd /opt/hikoutei-website
            git fetch --all
            git reset --hard "origin/${REF}"

            # Inject secrets (single source of truth: GH Secrets) — no sudo
            # needed: everything lives inside the repo checkout. The SA key is
            # passed as a single-line env value; the container materializes it
            # to its own tmpfs at startup (no key file on the host).
            install -d -m 700 demo/deploy/secrets
            printf '%s' "$DEMO_SA_JSON" | python3 -m json.tool > /dev/null
            MIN_JSON=$(printf '%s' "$DEMO_SA_JSON" | python3 -c 'import json,sys; print(json.dumps(json.load(sys.stdin), separators=(",", ":")))')
            {
              printf '%s\n' "$DEMO_ENV"
              printf 'DEMO_SA_JSON=%s\n' "$MIN_JSON"
            } > demo/deploy/secrets/demo.env
            chmod 600 demo/deploy/secrets/demo.env
            rm -f demo/deploy/secrets/sa.json

            # QA env (separate file — docker compose ${VAR} interpolation for
            # the qa service's environment block reads THIS shell's exported
            # vars at `up` time, so QA_SA_JSON must be EXPORTED here, not
            # only present in demo.env. Same key as demo (quota shared).
            export QA_SA_JSON="$MIN_JSON"
            export QA_SYNC_SPREADSHEET_URL="${QA_SYNC_SPREADSHEET_URL:-}"

            # Swap static docs in place (keep the directory inode — the
            # running caddy container holds the bind mount).
            mkdir -p demo/deploy/docs-dist
            tar -xzf /tmp/hikoutei-docs.tar.gz -C demo/deploy/docs-dist
            rm -f /tmp/hikoutei-docs.tar.gz

            docker compose -f demo/deploy/docker-compose.yml up -d --build --force-recreate

      - name: Health gate
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEMO_SSH_HOST }}
          username: ${{ secrets.DEMO_SSH_USER }}
          key: ${{ secrets.DEMO_SSH_KEY }}
          script: |
            set -euo pipefail
            for i in 1 2 3 4 5 6; do
              if curl -fsS --resolve hikoutei.cloud:443:127.0.0.1 https://hikoutei.cloud/api/health | grep -q '"syncMode":"sync"'; then
                echo "health OK: sync mode"
                exit 0
              fi
              sleep 5
            done
            echo "health check failed — demo server did not report sync mode"
            docker compose -f /opt/hikoutei-website/demo/deploy/docker-compose.yml logs --tail 50
            exit 1